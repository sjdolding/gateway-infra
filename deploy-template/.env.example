# Environment variables for project deployment
#
# Instructions:
#   1. Copy this file to .env in the same directory
#   2. Replace all placeholder values with real secrets
#   3. NEVER commit .env to git (add to .gitignore)

# App version (Git tag or commit SHA)
APP_VERSION=latest

# Database configuration
DB_NAME=myapp_db
DB_USER=myapp_user
DB_PASSWORD=change_me_strong_password_here

# API secrets (generate with: openssl rand -base64 32)
JWT_SECRET=change_me_jwt_secret_here
API_KEY=change_me_api_key_here

# Optional: add project-specific variables below
